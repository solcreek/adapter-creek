# Allow workerd/esbuild/sharp postinstall scripts (downloading binaries).
# Without these, miniflare/wrangler can't run workerd locally.
only-built-dependencies[]=workerd
only-built-dependencies[]=esbuild
only-built-dependencies[]=sharp
