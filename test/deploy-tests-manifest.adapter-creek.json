{
  "version": 2,
  "_comment": "Adapter-creek deploy-test exclusions. Merged with Next.js's canonical test/deploy-tests-manifest.json via NEXT_EXTERNAL_TESTS_FILTERS. Empty right now — there are no remaining cases that can't run on this adapter.",
  "suites": {},
  "rules": {
    "include": [],
    "exclude": []
  }
}
